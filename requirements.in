# What the code imports directly, pinned. `make lock` compiles it into
# requirements.txt: the whole tree with hashes, the only file the image installs
aiohttp==3.14.3
aiosqlite==0.22.1
google-genai==1.62.0
httpx==0.28.1
Pillow==12.3.0
python-dotenv==1.2.3
twitchio==3.2.1
