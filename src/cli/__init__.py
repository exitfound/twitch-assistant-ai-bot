"""Command-line commands (bot.py --…). The bot does not start for them.

    main.py       argument parsing and the commands themselves
    knowledge.py  lore import and cleanup
    emotes.py     emote sync from Twitch into CONTENT.md
    memory.py     --build-memory: the bot's memory over the whole history
"""
